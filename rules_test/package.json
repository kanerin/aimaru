{
  "name": "aimaru-rules-test",
  "private": true,
  "type": "module",
  "description": "Firestore / Storage セキュリティルールのテスト。エミュレータ上で実行する",
  "scripts": {
    "test": "firebase emulators:exec --project aimaru-test --only firestore,storage \"node --test --test-concurrency=1 *.test.js\""
  },
  "engines": {
    "node": "22"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.29.1"
  }
}
